# Same Express backend used by the main student/admin frontend.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Public site (main student/admin frontend) — used for "view public page" links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
